# Compiling on Windows with Visual Studio

> **This guide has been retired.** The legacy MSBuild project (`vc18/`) was removed in favour of the CMake-based build, which uses the same MSVC compiler and produces an identical binary.

Please follow the [Windows (CMake)](windows-cmake.md) guide instead.
